name: courier
description: Dart Courier RPC client — wire-compatible with Go courier (MQTT + Protobuf)
version: 0.1.0
homepage: https://github.com/simpossible/courier-flutter

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  mqtt_client: ^10.2.1
  protobuf: ^4.0.0
  fixnum: ^1.1.0

dev_dependencies:
  test: ^1.25.0
